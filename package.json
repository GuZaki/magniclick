{
  "name": "magniclick",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --open",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "sass-embedded": "^1.83.0",
    "scss": "^0.2.4",
    "vite": "^6.0.5"
  },
  "dependencies": {
    "normalize.css": "^8.0.1"
  }
}
